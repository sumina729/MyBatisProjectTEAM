<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Board">
    <insert id="insertBoard">
        insert into vocabulary (english, meaning, `explain`, level)
        values (#{english} , #{meaning} , #{explain}, #{level})
    </insert>
    <update id="updateBoard">
        update vocabulary
        set english=#{english}, meaning=#{meaning}, `explain`=#{explain}, level=#{level} where seq=#{seq}
    </update>
    <delete id="deleteBoard">
        delete from vocabulary where seq=#{seq}
    </delete>
    <select id="getBoard" resultType="board">
        select * from vocabulary where seq=#{seq}
    </select>
    <select id="getBoardList" resultType="board">
        select * from vocabulary order by seq desc
    </select>
</mapper>
        <!--english, meaning, explain, level-->